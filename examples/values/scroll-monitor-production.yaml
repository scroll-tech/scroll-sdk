ingress:
  main:
    ingressClassName: "nginx"
    hosts:
      - host: ""
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: "grafana"
                port:
                  number: 80