configMaps:
  env:
    enabled: true
    data:
      CHAIN_ID: "111111"

ingress:
  main:
    ingressClassName: "nginx"
    hosts:
      - host: l1-2.devnet.scroll.tech
        paths:
          - path: /
            pathType: Prefix
